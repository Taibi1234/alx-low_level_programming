#include "main.h"
/**
 * _islower - program checkes for lowercase charachter
 * @c: the charachter to check
 * Return: 1 if c is lowercase, and 0 if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
